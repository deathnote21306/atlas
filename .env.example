# --- Postgres (local docker-compose) ---
DATABASE_URL=postgresql+psycopg://atlas:atlas@localhost:5433/atlas

# --- Auth ---
JWT_SECRET=change-me-32-bytes-min
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=480
DEMO_USER_EMAIL=analyst@atlas.test
DEMO_USER_PASSWORD=change-me

# --- App ---
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:5173
LOG_LEVEL=INFO

# --- AI Scoring (optional — heuristic fallback if missing) ---
ANTHROPIC_API_KEY=

# --- Trade Data (optional — seed data shown if missing) ---
COMTRADE_API_KEY=
